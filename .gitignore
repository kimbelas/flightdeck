node_modules/
.next/
coverage/
dist/
out/
*.log
*.tsbuildinfo
.env
.env.*
fixtures/raw/
.vitest/
src-tauri/target/

# scripts/statusline-cost.py imports scripts/statusline-block.py to time it.
__pycache__/

# Runtime logs and local scratch
.flightdeck-core.log
.flightdeck-deck.log
*.local.mjs

# What `npm run smoke` leaves behind. CI uploads it as an artifact on failure (P2-T7).
smoke.png

# Local Claude Code overrides; the shared config is .claude/settings.json
.claude/settings.local.json

# The verbatim prompt log is kept locally, not published (D23).
PROMPTS.md

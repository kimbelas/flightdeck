node_modules/
.next/
coverage/
dist/
out/
*.log
# ...except the scrubbed daemon.log fixtures, which are a shape a parser reads (P7-T4, D60).
# The raw captures stay ignored: `fixtures/raw/` is excluded as a directory below.
!fixtures/daemon/*.log
*.tsbuildinfo
.env
.env.*
fixtures/raw/
.vitest/
src-tauri/target/

# scripts/statusline-cost.py imports scripts/statusline-block.py to time it.
__pycache__/

# Runtime logs and local scratch
.flightdeck-core.log
.flightdeck-deck.log
*.local.mjs

# What `npm run smoke` and `npm run smoke:dev` leave behind. CI uploads them on failure.
smoke.png
smoke-dev.png

# Local Claude Code overrides; the shared config is .claude/settings.json
.claude/settings.local.json

# The verbatim prompt log is kept locally, not published (D23).
PROMPTS.md
